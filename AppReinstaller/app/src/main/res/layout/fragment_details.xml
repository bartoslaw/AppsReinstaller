<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/darker_gray"
                android:orientation="vertical">

    <EditText
        android:id="@+id/fragment_details_url_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="@dimen/activity_horizontal_margin"
        android:hint="@string/url_input_hint"
        android:inputType="textUri"/>

    <Button
        android:id="@+id/fragment_details_install_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/fragment_details_url_input"
        android:layout_centerHorizontal="true"
        android:text="@string/reinstall_app_button_cta"/>

</RelativeLayout>